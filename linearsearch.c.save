#include <stdio.h>

int main()
{
  int nums[5] = {1,2,3,4,5};
  int target = 5;

  int size = sizeof(nums)/sizeof(nums[0]);

  for (int i = 0 ; i < size ; i++)
  {
    if(nums[i] == target)
    {
      return i;
    }
  }

  return 0;
}
